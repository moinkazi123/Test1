version: 0.2

phases:
  install:
    runtime-versions:
      dotnet: 8.0
    commands:
      - echo Installing dependencies...
      - dotnet restore

  build:
    commands:
      - echo Building the application...
      - dotnet publish -c Release -o output

artifacts:
  files:
    - output/**
